<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html>
  <head>
    <title>Talleres en la Casa | <%= h(yield(:title)) %></title>
    <%= stylesheet_link_tag 'reset', 'style' %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <h1>Talleres en la Casa | <%=h yield(:title) %></h1>
      <div id="background">
        <div id="content">
          <ul class="menu">
            <li><%= link_to 'Los Talleres', workshops_path %></li>
            <% if admin? %>
              <li><%= link_to 'Logout', logout_path %></li>
            <% else %>
              <li><%= link_to 'Login', login_path %></li>
            <% end %>
          </ul>
          
          <%- flash.each do |name, msg| -%>
            <%= content_tag :div, msg, :id => "flash_#{name}" %>
          <%- end -%>
      
          <%= yield %>
        </div> <!-- #content -->
      </div> <!-- background -->
    </div> <!-- #container -->
  </body>
</html>
